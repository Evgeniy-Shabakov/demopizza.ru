name: ${DOCKER_NAME}-client

services:
  client-service:
    build: .
    image: ${DOCKER_NAME}-client-image:latest
    container_name: ${DOCKER_NAME}-client-container
    restart: unless-stopped
    networks:
      - docker-network

networks:
  docker-network:
    external: true